# continuum-plugin-audiobooksdb

Local-filesystem audiobook backend for Continuum. Implements `audiobook_backend.v1` against a directory tree of M4B files. Sibling of `continuum-plugin-bookwarehouse-audio`.

See `docs/operations.md` for operator setup.
